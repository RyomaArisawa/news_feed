import 'package:flutter/material.dart';

class NewsWebPageScreen extends StatelessWidget {
  const NewsWebPageScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
